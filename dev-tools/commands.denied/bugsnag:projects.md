---
allowed-tools: Bash
description: List all available Bugsnag projects
---

Execute the bugsnag skill to display all Bugsnag projects.

Use Bash tool to run the bugsnag.rb script with the `projects` command. The script is located in the dev-tools plugin at `skills/bugsnag/bugsnag.rb`.

Display the output directly to the user without any additional commentary.
